# 📦 Публикация на GitHub Pages

## Быстрая публикация

### Вариант 1: Через веб-интерфейс GitHub

1. **Создайте новый репозиторий на GitHub:**
   - Перейдите на [github.com/new](https://github.com/new)
   - Название: `franchise-landing` (или любое другое)
   - Выберите **Public** или **Private**
   - НЕ добавляйте README, .gitignore или лицензию
   - Нажмите **Create repository**

2. **Загрузите файлы в репозиторий:**
   ```bash
   cd franchise
   git init
   git add .
   git commit -m "Initial commit: Franchise landing page"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/franchise-landing.git
   git push -u origin main
   ```
   Замените `YOUR_USERNAME` на ваш GitHub username.

3. **Включите GitHub Pages:**
   - Перейдите в **Settings** репозитория
   - Прокрутите до секции **Pages**
   - В **Source** выберите **Deploy from a branch**
   - Выберите ветку **main** и папку **/ (root)**
   - Нажмите **Save**

4. **Ваш сайт будет доступен по адресу:**
   ```
   https://YOUR_USERNAME.github.io/franchise-landing/
   ```

### Вариант 2: Через GitHub CLI

```bash
# Установите GitHub CLI (если еще не установлен)
# macOS: brew install gh
# Windows: winget install GitHub.cli
# Linux: sudo apt install gh

# Войдите в GitHub
gh auth login

# Перейдите в папку проекта
cd franchise

# Инициализируйте git репозиторий
git init
git add .
git commit -m "Initial commit: Franchise landing page"

# Создайте репозиторий и опубликуйте
gh repo create franchise-landing --public --source=. --remote=origin --push

# Включите GitHub Pages через веб-интерфейс или CLI
gh api repos/YOUR_USERNAME/franchise-landing/pages -X POST \
  -f source[ branch=main, path=/ ]
```

### Вариант 3: Используя готовый скрипт

Запустите скрипт `publish_github.sh`:
```bash
chmod +x publish_github.sh
./publish_github.sh
```

## Настройка кастомного домена

1. В настройках репозитория → **Pages** → **Custom domain**
2. Введите ваш домен (например: `franchise.example.com`)
3. Добавьте CNAME запись в DNS вашего домена:
   ```
   Type: CNAME
   Name: franchise (или @ для корневого домена)
   Value: YOUR_USERNAME.github.io
   ```

## Обновление сайта

После каждого изменения:
```bash
git add .
git commit -m "Update landing page"
git push
```

Изменения появятся на сайте через 1-2 минуты.

## Структура для GitHub Pages

GitHub Pages автоматически обслуживает статические файлы из корня репозитория или папки `docs/`.

Текущая структура идеальна:
```
franchise/
├── index.html      ← Главная страница
├── style.css       ← Стили
├── script.js       ← JavaScript
└── ...
```

## Troubleshooting

**Сайт не обновляется:**
- Подождите 1-2 минуты
- Очистите кеш браузера (Ctrl+Shift+R)
- Проверьте настройки Pages в Settings

**404 ошибка:**
- Убедитесь, что файл называется `index.html`
- Проверьте, что Pages включен в Settings
- Убедитесь, что выбрана правильная ветка (main)

**Стили не загружаются:**
- Проверьте пути к файлам (должны быть относительными)
- Убедитесь, что все файлы закоммичены в git

# 🚀 Инструкция по деплою

## Vercel (рекомендуется)

### Вариант 1: Через веб-интерфейс
1. Перейдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub/GitLab/Bitbucket
3. Нажмите **"New Project"**
4. Выберите репозиторий или загрузите папку `franchise`
5. Vercel автоматически определит настройки
6. Нажмите **"Deploy"**

### Вариант 2: Через CLI
```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Перейдите в папку проекта
cd franchise

# Запустите деплой
vercel

# Для продакшена
vercel --prod
```

## Netlify

### Вариант 1: Drag & Drop
1. Перейдите на [netlify.com](https://netlify.com)
2. Войдите в аккаунт
3. Перетащите папку `franchise` в область **"Deploy"**
4. Дождитесь завершения деплоя
5. Получите ссылку на сайт

### Вариант 2: Через Git
1. Загрузите проект в GitHub/GitLab/Bitbucket
2. Перейдите на [netlify.com](https://netlify.com)
3. Нажмите **"New site from Git"**
4. Выберите репозиторий
5. Настройки определятся автоматически
6. Нажмите **"Deploy site"**

### Вариант 3: Через CLI
```bash
# Установите Netlify CLI (если еще не установлен)
npm i -g netlify-cli

# Перейдите в папку проекта
cd franchise

# Войдите в Netlify
netlify login

# Запустите деплой
netlify deploy --prod
```

## GitHub Pages

1. Загрузите проект в репозиторий GitHub
2. Перейдите в **Settings** → **Pages**
3. Выберите ветку `main` и папку `/franchise`
4. Нажмите **Save**
5. Сайт будет доступен по адресу: `https://username.github.io/repository-name/`

## Настройка домена

### Vercel
1. В настройках проекта перейдите в **Domains**
2. Добавьте свой домен
3. Следуйте инструкциям по настройке DNS

### Netlify
1. В настройках сайта перейдите в **Domain settings**
2. Нажмите **Add custom domain**
3. Введите домен и следуйте инструкциям

## Проверка после деплоя

После деплоя проверьте:
- ✅ Все секции отображаются корректно
- ✅ Форма работает (проверьте консоль браузера)
- ✅ Адаптивность на мобильных устройствах
- ✅ Все ссылки работают
- ✅ Изображения загружаются

## Обновление сайта

### Vercel
- Автоматически обновляется при push в Git
- Или используйте `vercel --prod` для ручного обновления

### Netlify
- Автоматически обновляется при push в Git
- Или используйте `netlify deploy --prod` для ручного обновления

## Поддержка

При возникновении проблем:
1. Проверьте консоль браузера на ошибки
2. Проверьте логи деплоя в панели Vercel/Netlify
3. Убедитесь, что все файлы загружены корректно
